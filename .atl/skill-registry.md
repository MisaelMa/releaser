# Skill Registry — releaser

Auto-generated by `sdd-init` on 2026-05-04. Re-run `sdd-init` or `skill-registry` to refresh.

## Project Conventions

No project-level convention files detected (`CLAUDE.md`, `AGENTS.md`, `agents.md`, `GEMINI.md`, `.cursorrules`, `copilot-instructions.md`).

Global conventions (user-level `~/.claude/CLAUDE.md`):
- Conventional Commits only — never add `Co-Authored-By` or AI attribution
- Never build after changes
- Verify technical claims before stating them
- Senior architect tone — push back on shortcuts, explain WHY

## User Skills

| Skill | Triggers | Source |
|-------|----------|--------|
| branch-pr | Creating a PR, opening a PR, preparing changes for review | `~/.claude/skills/branch-pr/SKILL.md` |
| issue-creation | Creating a GitHub issue, reporting a bug, requesting a feature | `~/.claude/skills/issue-creation/SKILL.md` |
| judgment-day | "judgment day", "review adversarial", "dual review", "juzgar", "que lo juzguen" | `~/.claude/skills/judgment-day/SKILL.md` |
| skill-creator | Creating a new skill, adding agent instructions, documenting patterns for AI | `~/.claude/skills/skill-creator/SKILL.md` |
| go-testing | Go tests, Bubbletea TUI testing | `~/.claude/skills/go-testing/SKILL.md` (not applicable — Elixir project) |

## Compact Rules (inject into sub-agents)

### Project Standards (auto-resolved)

**Stack**: Elixir 1.17.3 / OTP 27 (per `.tool-versions`), Mix project. Single-app library targeting Hex publishing.

**Architecture**:
- Public API surface in `lib/releaser.ex` and `Releaser.*` modules under `lib/releaser/`
- Mix tasks live in `lib/mix/tasks/releaser.<name>.ex` and use the `Mix.Tasks.Releaser.<Name>` namespace
- Hook implementations in `lib/releaser/hooks/` (PreHook, PostHook, GitTag, ChangelogHook)
- Domain split (per `mix.exs` doc groups): Core, Dependency Graph (Graph, Cascade), Publishing (Publisher, HexStatus), Changelog (Changelog, FileSync), Hooks, Git, Mix Tasks, Utilities (UI)
- Tests mirror lib structure under `test/releaser/<module>_test.exs` using ExUnit

**Testing**:
- Run with `mix test`
- ExUnit only — no integration/E2E tooling installed
- Coverage available via built-in `mix test --cover`
- Strict TDD Mode is **ENABLED** — write a failing test first, then implementation

**Conventions**:
- Conventional Commits MANDATORY — validated by `.githooks/commit-msg` running `mix releaser.check_commit_msg`
- Bump rules: `feat` → minor, `fix` / `perf` / `refactor` / `revert` → patch, breaking (`!` or `BREAKING CHANGE:` body) → major
- Scope aliases: `rel` / `release` → `releaser`
- Max commit subject length: 100 chars; strict types and scopes; `no_scope` allowed (applies to `releaser`)
- Never add `Co-Authored-By` or AI attribution to commits
- Module docs use `@moduledoc` with markdown sections; doc groups configured in `mix.exs` `groups_for_modules`

**Quality Tools**:
- Formatter: `mix format` (built-in; no `.formatter.exs` present — uses defaults)
- Linter: NOT INSTALLED (no credo)
- Type checker: NOT INSTALLED (no dialyxir)

**CI/CD**:
- GitHub Actions: `.github/workflows/release.yml` runs on push to `dev`, `beta`, `main`
- Pre-release tags: `dev` and `beta` branches; stable bumps on `main`
- Hex publish gated by `vars.PUBLISH_TO_HEX` and `secrets.HEX_API_KEY`

### When creating a PR
Apply `branch-pr` patterns. Issue-first: ensure a tracking issue exists.

### When creating an issue
Apply `issue-creation` patterns.

### When asked for adversarial / dual review
Apply `judgment-day` protocol.
